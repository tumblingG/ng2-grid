import { SlVirtualScrollDirective } from './sl-virtual-scroll.directive';

describe('SlVirtualScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SlVirtualScrollDirective();
    expect(directive).toBeTruthy();
  });
});
