import { TrDirective } from './tr.directive';

describe('TrDirective', () => {
  it('should create an instance', () => {
    const directive = new TrDirective();
    expect(directive).toBeTruthy();
  });
});
