import { TbodyDirective } from './tbody.directive';

describe('TbodyDirective', () => {
  it('should create an instance', () => {
    const directive = new TbodyDirective();
    expect(directive).toBeTruthy();
  });
});
