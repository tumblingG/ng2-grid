import { ClasslistAddDirective } from './classlist-add.directive';

describe('ClasslistAddDirective', () => {
  it('should create an instance', () => {
    const directive = new ClasslistAddDirective();
    expect(directive).toBeTruthy();
  });
});
